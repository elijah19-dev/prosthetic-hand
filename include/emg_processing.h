#pragma once

#include "system_types.h"

// TODO: Define a pure raw-reading-to-EMGProcessed interface after measuring
// relaxed and contracted MyoWare ENV signals. Reject invalid readings before
// they can become commands; normalization and filter parameters remain TBD.
